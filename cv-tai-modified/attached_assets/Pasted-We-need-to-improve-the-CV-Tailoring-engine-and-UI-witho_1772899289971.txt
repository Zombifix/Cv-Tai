We need to improve the CV Tailoring engine and UI without breaking the current architecture.

Do NOT refactor the entire system. Only improve the following areas.

--------------------------------------------------

1️⃣ Language Detection (CV must match job language)

Before generating the CV, detect the language of the job description.

Rules:
- If the job description is mostly French → generate the CV in French
- If the job description is mostly English → generate the CV in English

The following must follow the detected language:
- summary
- bullet rewriting
- skills wording
- section titles

Add this to the Optimization Report:
Detected language: FR / EN

--------------------------------------------------

2️⃣ Fix "Detected Keywords" panel (currently empty)

The system already extracts keywords internally but they are not displayed.

Update the Optimization Report to include:

Detected Keywords:
- Required skills
- Preferred skills
- Responsibilities
- Domain keywords

Make sure these are taken from the job parsing step and exposed to the UI.

--------------------------------------------------

3️⃣ Improve Experience Ranking (brand / industry signal)

Experience ranking currently uses:
- role similarity
- skill matching
- recency

Add two additional signals:

Industry similarity
Example:
Bank job → Natixis experience becomes more relevant.

Brand proximity
If company brand is recognized or similar domain:
increase score slightly.

Scoring update example:

experience_score =
role_match
+ skill_match
+ recency
+ industry_similarity
+ brand_proximity

Do NOT overweight prestige brands. Keep it balanced.

--------------------------------------------------

4️⃣ Improve CV composition rules

Current output is sometimes too dense.

Enforce strict limits:

- maximum 3 experiences
- maximum 3 bullets per experience
- maximum 8 bullets total

Sort experiences by relevance score.

Never include debug words like:
Fallback
Debug
Internal

--------------------------------------------------

5️⃣ Improve CV readability in UI

The generated CV currently looks like raw markdown.

Improve visual formatting:

Structure:

TITLE

SUMMARY

EXPERIENCE

Company — Role  
Dates  
• bullet  
• bullet  

Company — Role  
Dates  
• bullet  

SKILLS

UI improvements:
- increase vertical spacing
- bold section titles
- slightly larger company names
- separate sections with visual spacing

Do NOT change the data model.

--------------------------------------------------

6️⃣ Improve Optimization Report readability

The right panel is too compressed.

Improve layout:

Selected Experiences
- each experience as a card
- show score
- show reason tags (ROLE, INDUSTRY, SKILLS, RECENCY)

Selected Bullets
- collapsible sections per experience
- show score and matching keywords

Add more vertical spacing between cards.

--------------------------------------------------

7️⃣ Add Groq verification endpoint

We must confirm the LLM provider is actually Groq.

Create endpoint:

/api/llm/health

It should:
- call the current LLM provider
- prompt: "Reply exactly with OK"
- return:

{
 provider
 model
 base_url
 response_time_ms
 response_text
}

Log this server-side.

--------------------------------------------------

8️⃣ Stability rule

The CV generator must never crash.

If no bullet passes the relevance threshold:

fallback to the top bullets of the highest-ranked experience.

Do not return an error.

--------------------------------------------------

Goal:
Improve usability, explainability, and output quality while keeping the existing architecture intact.
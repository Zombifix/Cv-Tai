## Packages
(none needed)

## Notes
- Wouter handles routing.
- Using Shadcn UI components provided in the workspace.
- The Tailwind config should be fully respected, specifically `--font-display` and `--font-sans`.
- `api.bullets.listByExperience` uses `buildUrl` to inject `experienceId`.
